# StockAI 跨平台打包与独立分发使用指南

本软件使用 **PyInstaller** 深度打包，产物为完全自包含的独立分发程序，**内嵌了完整的 Python 运行时、PySide6 图形驱动库、所有依赖三方包及 SSL 根证书**。直接拷贝到任意未安装 Python 环境的其它电脑上即可双击直接运行。

---

## 1. Windows 独立运行包使用方式

### 1.1 产物路径与目录结构
本地构建完成后，输出物位于工作区根目录的：
```text
dist/StockAI/
├── StockAI.exe            # 主执行程序（双击即可启动）
├── PySide6/               # Qt 运行时及图形渲染插件
├── certifi/               # 内嵌 SSL 根证书（保障 HTTPS 请求不报错）
├── python313.dll          # 独立 Python 解释器动态库
└── ... (其余自包含依赖库)
```

### 1.2 复制到其它电脑运行
1. 将 `dist/StockAI/` 整个文件夹压缩为 `StockAI-Windows-x64.zip`；
2. 拷贝到任意 Windows 10 / 11 电脑（无需安装 Python、无需配置任何环境）；
3. 解压后直接双击进入文件夹中的 **`StockAI.exe`** 即可直接开启股票智能分析终端。

---

## 2. macOS 苹果电脑打包与使用方式

> **跨平台技术规范说明**：由于操作系统底层二进制架构限制（Windows PE 与 macOS Mach-O 动态库不可跨系统交叉编译），macOS 的 `.app` 应用程序包必须在 macOS 环境中生成。

### 方式 A：在任意 Mac 电脑上一键本地生成（推荐）
若您或团队成员拥有一台 macOS 电脑（Intel 或 Apple Silicon M1/M2/M3/M4）：
1. 克隆或拷贝本项目代码至 Mac 电脑；
2. 打开 macOS 终端进入项目根目录，安装依赖：
   ```bash
   pip3 install PySide6 pyqtgraph pandas numpy akshare openai cryptography platformdirs requests certifi pydantic pyinstaller
   ```
3. 运行已调优的统一打包指令：
   ```bash
   pyinstaller --clean stock_ai.spec
   ```
4. 构建完成后，在 `dist/` 目录下即可直接得到独立的 **`StockAI.app`** 应用程序包，拖入“应用程序 (Applications)”目录即可直接启动。

---

### 方式 B：通过 GitHub Actions 云端全自动免机构建（零门槛云构建）
项目根目录下已为您内置了 [`.github/workflows/build_release.yml`](file:///d:/AI/stockAnalasis/.github/workflows/build_release.yml) 自动化构建工作流：
1. 将本项目推送至您的 GitHub 仓库；
2. 每次打 Tag（如 `v1.0.0`）或在 Actions 页面手动点击 `Run workflow`；
3. GitHub 将自动调配一台 Windows 云主机与一台 macOS 云主机并行打包；
4. 构建完毕后，您可直接在 Actions 页面一键下载已经打好包的 `StockAI-Windows-x64.zip` 与 `StockAI-macOS.zip`。
